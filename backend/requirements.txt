fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
# SQLAlchemy needs greenlet for its async engine, which is what ADK's
# DatabaseSessionService (app/chat_runtime.py) builds on top of DATABASE_URL.
# SQLAlchemy declares it optional, so nothing pulls it in on its own and the
# first chat turn fails with "the greenlet library is required".
greenlet==3.5.5
pydantic==2.13.5
pydantic[email]==2.13.5
cryptography==44.0.0
python-multipart==0.0.20
python-dotenv==1.0.1
psycopg[binary]==3.2.3
pypdf==5.1.0
python-docx==1.1.2
google-adk[mcp]==2.8.0
# Pinned below the latest (1.100.1 at time of writing): that release and
# 1.98.0-1.99.0 unconditionally import an Anthropic module that uses
# typing.NotRequired, which only exists in the stdlib from Python 3.11 --
# breaks on our Python 3.10 venv regardless of which provider is actually
# used. 1.96.0 is the newest version confirmed not to hit this.
litellm==1.96.0